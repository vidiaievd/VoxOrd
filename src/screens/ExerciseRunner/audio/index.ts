/**
 * The listening layer on the phone, student side — plan 56 phase 7.
 *
 * Exercise-type agnostic to a fault: nothing here knows what an item is, which is what
 * lets one player mount on every template that carries a clip.
 */
export { ExerciseAudioPlayer } from './ExerciseAudioPlayer';
export { AudioGateScreen, AudioLockNote, AudioSegmentButton, AudioTranscript } from './AudioParts';
